/**
 * @auction-radar/enrich — 실거래가 결합 · 중위값 · 신뢰도 등급. (기획서 §6.6)
 *
 * v1.x 범위. 현재는 빌드 가능한 빈 스텁이며, 드라이버 인터페이스 seam 만 예약한다.
 */

/** 패키지 버전 마커 (스텁). */
export const ENRICH_PACKAGE_VERSION = '0.1.0' as const;
