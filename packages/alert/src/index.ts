/**
 * @auction-radar/alert — 매칭 엔진 + notifier(telegram) + digest + quiet hours. (기획서 §6.4, §6.5)
 *
 * SPEC-ALERT-001 범위. 현재는 빌드 가능한 빈 스텁이며, events/스토어 소비 seam 만 예약한다.
 */

/** 패키지 버전 마커 (스텁). */
export const ALERT_PACKAGE_VERSION = '0.1.0' as const;
