# ── 민감정보 (절대 커밋 금지) ─────────────────
.env
.env.*
!.env.example
*.pem
*.key
config.json          # 실제 사용자 설정 (토큰 포함 가능)
secrets/
credentials*

# ── 로컬 데이터 ──────────────────────────────
*.db
*.db-journal
*.sqlite
*.sqlite3
data/

# ── 로컬 Claude / 에디터 설정 ─────────────────
.claude/settings.local.json
.vscode/
.idea/
.DS_Store

# ── Node ────────────────────────────────────
node_modules/
dist/
build/
coverage/
*.log
*.tsbuildinfo
